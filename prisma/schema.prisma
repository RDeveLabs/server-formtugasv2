generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
  url = env("DATABASE_URL")
}

model kelas{
  id Int @id @default(autoincrement())
  kelas String @unique
  file file[]
}

model file {
  id Int @id @default(autoincrement())
  nama String 
  nim Int
  dari_pertemuan Int
  sampai_pertemuan Int
  waktu DateTime @default(now())
  ukuran_file Int?
  kelas kelas @relation(fields: [id_kelas], references: [id])
  id_kelas Int
}